name: AI Critic (Issues) - Gemini

on:
  issues:
    types: [opened, reopened]
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  analyze_issue:
    runs-on: ubuntu-latest
    steps:
      - name: Ask AI critic (Gemini)
        id: ai-response
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ISSUE_USER: ${{ github.event.issue.user.login }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          SYSTEM_PROMPT="You are a helpful issue triage assistant for a Python Shiny dashboard project (fin-health).
          Your task is to analyze newly opened issues for completeness and clarity.

          If the issue is clear, detailed, and includes reproducibility steps, thank the contributor and confirm the issue is well-documented.

          If the issue is vague, missing steps to reproduce, or poorly formatted, politely point out what's missing. Offer specific suggestions on how to improve it, such as adding environment details, expected vs. actual behavior, browser info, or screenshots of the dashboard.

          Be encouraging and constructive—remember that contributors may be new to open source.

          Identify any missing information that would help resolve the issue faster.
          Always respond with ready-to-use markdown content."

          jq -n \
            --arg system_prompt "$SYSTEM_PROMPT" \
            --arg user "$ISSUE_USER" \
            --arg title "$ISSUE_TITLE" \
            --arg body "$ISSUE_BODY" \
            '{
              contents: [{ parts: [{ text: ("New issue was opened by " + $user + "\nTitle: " + $title + "\nBody:\n---\n" + $body + "\n---") }] }],
              system_instruction: { parts: [{ text: $system_prompt }] }
            }' > payload.json

          RESPONSE=$(curl -s -X POST \
            -H "Content-Type: application/json" \
            -H "x-goog-api-key: $GEMINI_API_KEY" \
            -d @payload.json \
            "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent")

          REVIEW=$(echo "$RESPONSE" | jq -r '.candidates[0].content.parts[0].text // "Error: Failed to get response from Gemini. Check logs for details."')

          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "response<<$EOF" >> "$GITHUB_OUTPUT"
          echo "$REVIEW" >> "$GITHUB_OUTPUT"
          echo "$EOF" >> "$GITHUB_OUTPUT"

      - name: Comment results on the issue
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          body: ${{ steps.ai-response.outputs.response }}
